pluginManagement {
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }  // If using JitPack dependencies
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }  // If using JitPack dependencies
    }
}

include ':app'
rootProject.name = "nutritrack"
